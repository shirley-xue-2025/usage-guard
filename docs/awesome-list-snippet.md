# Snippet for awesome-list PRs

Search GitHub for: `awesome claude code` or `awesome claude`

Add under a **Tools** or **Claude Code** section:

```markdown
- [usage-guard](https://github.com/shirley-xue-2025/usage-guard) — Proactive 5-hour usage guard for Claude Code on macOS (Fable/long subagent runs). `/usage-guard` skill + external daemon; pause before extra wallet, resume after reset.
```

One-line version:

```markdown
- [usage-guard](https://github.com/shirley-xue-2025/usage-guard) — proactive 5h pause guard for Claude Code (macOS, Fable-friendly)
```
